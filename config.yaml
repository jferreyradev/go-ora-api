oracle:
  user: usuario
  password: contraseña
  host: localhost
  port: 1521
  service: servicio_o_sid

api:
  token: tu_token_seguro
  allowed_ips:
    - 127.0.0.1
    - ::1
    - localhost
  no_auth: false

server:
  port: 8080
